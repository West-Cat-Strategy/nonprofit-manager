/**
 * MODULE-OWNERSHIP: builder compatibility page wrapper
 *
 * Canonical implementation lives in `frontend/src/features/builder/pages/TemplatePreviewPage.tsx`.
 *
 * @deprecated Import the feature-owned page when touching builder routes.
 */
export { default } from '../../features/builder/pages/TemplatePreviewPage';
