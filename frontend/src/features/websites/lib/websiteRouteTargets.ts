export const getWebsiteOverviewPath = (siteId: string): string => `/websites/${siteId}/overview`;

export const getWebsiteContentPath = (siteId: string): string => `/websites/${siteId}/content`;

export const getWebsiteFormsPath = (siteId: string): string => `/websites/${siteId}/forms`;

export const getWebsiteNewslettersPath = (siteId: string): string =>
  `/websites/${siteId}/newsletters`;

export const getWebsiteIntegrationsPath = (siteId: string): string =>
  `/websites/${siteId}/integrations`;

export const getWebsitePublishingPath = (siteId: string): string =>
  `/websites/${siteId}/publishing`;

export const getWebsiteBuilderPath = (siteId: string): string => `/websites/${siteId}/builder`;
