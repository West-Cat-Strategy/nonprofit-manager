/**
 * MODULE-OWNERSHIP: builder route-owned page facade
 *
 * Legacy facade for compatibility mounts while route-mounted ownership
 * completes in the feature module.
 *
 * @deprecated Route consumers should migrate to a feature-owned page implementation.
 */
export { default } from '../../../pages/builder/TemplateGallery';
