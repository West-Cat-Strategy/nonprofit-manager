# P4-T41 Closeout

**Date:** 2026-04-14  
**Task resolved:** `P4-T41`

## Summary

- Verified production CORS alignment across the backend runtime, public-site runtime, compose defaults, env example, and deployment documentation.
- No runtime or docs changes were required for the validated contract, so this closeout records proof-only resolution rather than a config patch.
- The active review-row evidence is that the production CORS settings already point each surface at its intended origin and the backend security checks pass with the current configuration.

## Alignment Notes

- `backend/src/index.ts` reads `CORS_ORIGIN` for the API backend and normalizes the configured origin list before enforcing CORS.
- `backend/src/public-site.ts` does the same for the public-site backend, with the public-site-local default origin set to `http://localhost:8006,http://127.0.0.1:8006`.
- `docker-compose.yml` keeps the backend and public-site defaults distinct:
  - backend: `CORS_ORIGIN: ${CORS_ORIGIN:-http://localhost:8001,http://127.0.0.1:8001}`
  - public-site: `CORS_ORIGIN: ${PUBLIC_SITE_CORS_ORIGIN:-https://sites.westcat.ca}`
- `backend/.env.example` documents the local-development CORS origin as `HTTP://localhost:5173`.
- `docs/deployment/DEPLOYMENT.md` uses `CORS_ORIGIN=https://westcat.ca` in the production examples, matching the documented public origin.

## Validation

- `cd backend && npm run type-check`
  - Result: Passed.
- `cd backend && npx jest --runInBand src/__tests__/config/productionSecurityConfig.test.ts src/__tests__/config/database.test.ts`
  - Result: Passed. `2` suites, `9` tests.

## Conclusion

- The reviewed runtime and docs surfaces are aligned for the `P4-T41` CORS contract.
- Because the targeted verification stayed green, no code changes were necessary and the row can be resolved on evidence alone.
