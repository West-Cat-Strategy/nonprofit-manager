# P4-T37 / P4-T38 Closeout

**Last Updated:** 2026-04-19


**Date:** 2026-04-14  
**Tasks moved to Review:** `P4-T37`, `P4-T38`

## Summary

- Captures the review-ready closure evidence for the Phase 4 admin-panel and contacts cleanup rows.
- `P4-T37` is represented by manifest-driven admin/portal route wiring plus canonical frontend role normalization across the admin settings surfaces.
- `P4-T38` is represented by the feature-owned contacts seam, the remaining contacts-to-cases bridge cleanup, and the removal of the mixed legacy contact-detail ownership path.
- The validation matrix below records the targeted runs used to move both rows to Review without overstating repo-wide green status.

## Implementation Notes

- `P4-T37`: the route manifest now owns the admin and portal route shape, including the compatibility redirects and panel metadata needed by the route composer.
- `P4-T37`: role-sensitive admin UI now normalizes first and labels second, so `NavigationSettingsPage`, `UsersSection`, `UserManagement`, and the shared admin-settings helpers agree on the same canonical role slug.
- `P4-T38`: the contacts feature owns the document and note seams, the remaining contacts-to-cases bridge is isolated behind contacts-owned adapters/hooks, and the contact detail page no longer mixes in legacy root widgets.
- `P4-T38`: the workflow-side contact lookup is routed through the contacts-owned boundary instead of importing directly from the cases slice.

## Validation

- `cd frontend && npm run type-check`
  - Result: Passed.
- `cd frontend && npm test -- --run src/routes/__tests__/adminRedirects.test.tsx src/routes/__tests__/setupRedirects.test.tsx src/features/adminOps/components/__tests__/AdminPanelNav.test.tsx src/features/adminOps/pages/__tests__/AdminSettingsPage.test.tsx src/features/adminOps/pages/portalAdmin/PortalAdminPage.test.tsx src/features/auth/state/__tests__/adminAccess.test.ts src/features/adminOps/pages/adminSettings/__tests__/utils.test.ts src/components/__tests__/UserManagement.test.tsx`
  - Result: Passed. `8` files, `68` tests.
- `cd frontend && npm test -- --run src/features/contacts/components/__tests__/ContactDocumentsPanel.test.tsx src/features/contacts/components/__tests__/ContactNotesPanel.test.tsx src/features/contacts/pages/__tests__/ContactDetailPage.test.tsx`
  - Result: Covered by the broader contacts rerun below.
- `cd frontend && npm test -- --run src/features/contacts/components/__tests__/ContactDocumentsPanel.test.tsx src/features/contacts/components/__tests__/ContactNotesPanel.test.tsx src/features/contacts/pages/__tests__/ContactDetailPage.test.tsx src/features/contacts/pages/__tests__/ContactPrintPage.test.tsx src/features/contacts/components/__tests__/ContactCommunicationsPanel.test.tsx src/components/__tests__/ContactRelationships.test.tsx`
  - Result: Passed. `6` files, `14` tests.
- `cd backend && npm run type-check`
  - Result: Passed.
- `cd backend && npx jest --runInBand src/modules/contacts/controllers/__tests__/documents.controller.test.ts`
  - Result: Passed. `1` suite, `3` tests.
- `make check-links`
  - Result: Passed. Checked `90` files and `674` local links with no broken active-doc links.
- `cd e2e && npm run test:smoke`
  - Result: Not run for this closeout.
- The full `make ci-full` rerun is now green on 2026-04-14 after the shared `P4-T9H` dependency-audit follow-up, so there is no repo-wide blocker attached to these two rows.

## Residual Risk

- This closeout still relies on the targeted route/admin/contacts/backend slices above for row-local evidence, even though the repo-wide `make ci-full` rerun is now green.

## Addendum

- The live workboard update in [planning-and-progress.md](planning-and-progress.md) points back to this note so the closure remains traceable without duplicating the whole evidence chain.
