# P4-T1 Closeout

**Last Updated:** 2026-04-19


**Date:** 2026-04-13  
**Tasks moved to Review:** `P4-T1`, `P4-T1R4W3G`, `P4-T1R7`, `P4-T1R7C`, `P4-T1R7D`
**Shared closeout coverage reused for 2026-04-14 workboard closure:** `P4-T1R7A`, `P4-T1R7B`, `P4-T1R8`, `P4-T1R8A`, `P4-T1R8B`

## Summary

- Closed the remaining `builder` seam by wiring `@nonprofit-manager/contracts` into the backend and frontend workspace manifests and refreshing the install.
- Added page-level verification for the staff portal-admin surface with [`PortalAdminPage.test.tsx`](../../frontend/src/features/adminOps/pages/portalAdmin/PortalAdminPage.test.tsx).
- Removed stale payment-module comments that still referenced a deleted root shim file and re-confirmed the live tombstone contract.
- Kept intentional compatibility behavior in place: legacy payment and portal endpoints still tombstone to `/api/v2/*`, and legacy admin/settings routes still redirect to their canonical destinations.
- This same compatibility and dead-wrapper evidence also backs the 2026-04-14 closure of `P4-T1R7A`, `P4-T1R7B`, `P4-T1R8`, `P4-T1R8A`, and `P4-T1R8B`, with the current tree still matching the route and page-retirement notes recorded below.

## Implementation Notes

- Shared contracts fix used:
  `backend/package.json` and `frontend/package.json` now declare `@nonprofit-manager/contracts` via `file:../contracts`.
- Workspace note:
  the original `workspace:*` plan could not be applied on this machine because `npm 11.12.1` returned `EUNSUPPORTEDPROTOCOL` for the `workspace:` specifier during `npm install`.
- Shared contract package metadata:
  `contracts/package.json` now exposes `./messaging` and `./websiteBuilder` subpaths for type resolution.
- Portal-admin coverage:
  added a focused render test for the route shell, selected panel content, and portal-mode admin nav wiring.
- Redirect-spec cleanup:
  updated [`frontend/src/routes/__tests__/adminRedirects.test.tsx`](../../frontend/src/routes/__tests__/adminRedirects.test.tsx) so legacy routes that intentionally land on `/dashboard` assert the redirect contract instead of waiting for a nonexistent destination heading in the test harness.
- Payment compatibility cleanup:
  [`backend/src/modules/payments/index.ts`](../../backend/src/modules/payments/index.ts) and [`backend/src/modules/payments/routes/index.ts`](../../backend/src/modules/payments/routes/index.ts) now match the live tree: there is no `backend/src/routes/payments.ts` shim.

## Compatibility Confirmations

- Legacy tombstones intentionally retained:
  `/api/payments/*`, `/api/portal/auth/*`, and `/api/portal/admin/*`.
- Canonical API surfaces remain:
  `/api/v2/admin/*`, `/api/v2/portal/*`, `/api/v2/portal/auth/*`, `/api/v2/portal/admin/*`, `/api/v2/payments/*`.
- Admin/settings compatibility redirects intentionally retained:
  `/email-marketing`, `/settings/organization`, `/admin/audit-logs`, `/settings/admin`, and `/settings/admin/portal`.
- Current tree evidence:
  `backend/src/routes` contains only `health.ts` and `v2/`.
- Current tree evidence:
  `find frontend/src/pages -type f | wc -l` returned `0`.

## Validation

### Install / Bootstrap

- `npm install`
  Passed after switching the shared-contract dependency from the unsupported `workspace:*` form to `file:../contracts`.

### P4-T1R4W3G

- `cd frontend && npm run type-check`
  Passed.
- `cd frontend && npm test -- --run src/routes/__tests__/adminRedirects.test.tsx src/routes/__tests__/setupRedirects.test.tsx src/features/adminOps/pages/adminSettings/hooks/__tests__/adminSettingsHooks.test.ts src/features/portal/pages/__tests__/PortalSelfServicePages.test.tsx src/features/portal/pages/__tests__/PortalWorkflowPages.test.tsx src/features/adminOps/pages/portalAdmin/PortalAdminPage.test.tsx`
  Passed. `6` files, `51` tests.
- `cd frontend && npm test -- --run src/test/ux/RouteUxSmokeExtended.test.tsx src/test/ux/RouteUxSmoke.test.tsx`
  Passed. `2` files, `35` tests.
- `cd backend && npx jest --runInBand src/__tests__/integration/routeGuardrails.test.ts src/__tests__/integration/portalAppointments.test.ts src/__tests__/integration/portalMessaging.test.ts src/modules/portalAuth/controllers/__tests__/portalAuthController.test.ts`
  Passed. `4` suites, `87` tests.

### P4-T1R7

- `cd backend && npm run type-check`
  Passed.
- `cd frontend && npm run type-check`
  Passed.
- `cd backend && npx jest --runInBand src/__tests__/modules/payments.routes.security.test.ts src/__tests__/integration/legacyApiTombstone.test.ts`
  Passed. `2` suites, `18` tests.

### Docs

- `make check-links`
  Passed.
- `make lint-doc-api-versioning`
  Passed.
- `make lint`
  Failed on an unrelated repo policy gate: `schema_migrations INSERT tuples do not match the manifest canonical filenames or IDs.`

## Residual Risk

- This closeout used the scoped matrix above rather than `make test`, `cd e2e && npm run test:smoke`, or `make db-verify`.
- The shared-contract dependency uses `file:../contracts` for npm compatibility on this machine; if the repo standardizes on a newer workspace-aware npm flow later, the manifest spec can be normalized separately without reopening these tasks.
- Repo-native lint is still blocked by the migration-manifest policy drift above. That issue did not touch the `P4-T1` / `P4-T1R7` / `P4-T1R4W3G` seams and should stay with the owning migration-maintenance follow-up.

## 2026-04-14 Addendum

- `P4-T1R7A` and `P4-T1R7B` close against the same contacts/cases/outcomes compatibility evidence as `P4-T1R7`; the live tree still matches the module-owned backend seams and the feature-owned notes/report cutover described by the workboard closure batch.
- `P4-T1R8`, `P4-T1R8A`, and `P4-T1R8B` close against the same wrapper-retirement baseline captured here: `backend/src/routes` still contains only `health.ts` and `v2/`, and `frontend/src/pages` remains empty while route/store ownership stays feature-owned in the current tree.
- The broader row-specific proof for those closures is preserved in [P4_CLOSEOUT_BATCH_2026-04-14.md](P4_CLOSEOUT_BATCH_2026-04-14.md) so the live workboard can drop the now-complete rows without losing the evidence chain.
