# Repo Audit Remediation Tracker (2026-04-18)

This document is the live remediation companion to [repo-audit-2026-04-18.md](repo-audit-2026-04-18.md). The original audit remains an immutable snapshot; this tracker records the current implementation state, owner rows, verification commands, and closeout dates for each finding.

## Status Key

- `open`: not yet remediated in the current worktree
- `partial`: code and/or targeted validation landed, but final proof is still blocked or incomplete
- `closed`: implementation and the targeted verification slice for this finding are complete

## Findings

| Finding | Status | Owner row(s) | Current remediation state | Verification | Closeout date |
|---|---|---|---|---|---|
| 1. Staff session organization context can fail open | `closed` | `P4-T45` | Staff session issuance now derives the active organization from authenticated access instead of the old global default-org lookup, and request-time organization validation is tightened. The auth integration fixtures were updated to seed real org access under the new membership model. | `node scripts/check-auth-guard-policy.ts`; `cd backend && DB_HOST=127.0.0.1 DB_PORT=8012 DB_NAME=nonprofit_manager_test DB_USER=postgres DB_PASSWORD=postgres npx jest src/__tests__/integration/authorization.test.ts src/__tests__/services/authGuardService.test.ts src/__tests__/utils/permissions.test.ts --runInBand` | 2026-04-18 |
| 2. Backend compile/full-suite invariants are broken | `closed` | `P4-T48`, `P4-T7C` | Cases compile drift in `lifecycleQueries`, `caseServicesRepository`, and `caseService` is fixed. The stale reminder expectation was updated for nullable `occurrence_id`, the cases integration setup grants explicit org access under the new auth model, and the broader backend proof slice is green again. | `cd backend && npm run lint`; `cd backend && npm run type-check`; `cd backend && npx jest --runInBand src/__tests__/modules/mailchimp.routes.security.test.ts src/__tests__/modules/payments.routes.security.test.ts src/__tests__/modules/payments.intentOwnership.test.ts src/__tests__/services/authGuardService.test.ts src/__tests__/middleware/apiKeyAuth.test.ts`; `cd backend && DB_HOST=127.0.0.1 DB_PORT=8012 DB_NAME=nonprofit_manager_test DB_USER=postgres DB_PASSWORD=postgres npx jest --runInBand src/__tests__/integration/authorization.test.ts src/__tests__/utils/permissions.test.ts src/__tests__/integration/portalAuth.test.ts src/__tests__/integration/cases.test.ts src/__tests__/services/eventReminderAutomationService.test.ts` | 2026-04-18 |
| 3. Mailchimp integration routes are authenticated but not permission-gated | `closed` | `P4-T45`, `P4-T1R4W3D` | Mailchimp routes now require explicit admin-settings permission while preserving the public webhook route ordering. Dedicated route-security coverage was added. | `cd backend && npm test -- --runInBand src/__tests__/modules/mailchimp.routes.security.test.ts` | 2026-04-18 |
| 4. Payment intent read and cancel endpoints are auth-only | `closed` | `P4-T45` | Payment-intent read/cancel routes now require explicit permissions, and the controller enforces active-organization plus donation ownership before touching provider state. | `cd backend && npm test -- --runInBand src/__tests__/modules/payments.routes.security.test.ts src/__tests__/modules/payments.intentOwnership.test.ts` | 2026-04-18 |
| 5. Portal sessions are not revalidated against current portal-user state | `closed` | `P4-T45` | Portal auth middleware now rechecks current portal-user state before accepting a session, so suspended or otherwise inactive portal users lose access without waiting for token expiry. | `cd backend && DB_HOST=127.0.0.1 DB_PORT=8012 DB_NAME=nonprofit_manager_test DB_USER=postgres DB_PASSWORD=postgres npx jest src/__tests__/integration/portalAuth.test.ts --runInBand` | 2026-04-18 |
| 6. User settings can overwrite server truth after a transient load failure | `closed` | `P4-T7C` | Failed profile fetches no longer establish a save baseline from fallback data. The settings page now renders a retry banner and keeps saving disabled until authoritative data loads. | `cd frontend && npm run type-check`; `cd frontend && npm test -- --run src/features/adminOps/pages/__tests__/UserSettingsPage.test.tsx` | 2026-04-18 |
| 7. Navigation preference cache survives logout inside the same SPA session | `closed` | `P4-T7` | Logout and auth rejection now clear the real navigation-preferences cache, not just the test-only reset path. | `cd frontend && npm test -- --run src/hooks/__tests__/useNavigationPreferences.test.tsx` | 2026-04-18 |
| 8. Portal API requests inherit the staff organization header from local storage | `closed` | `P4-T7` | The portal API client now opts out of `X-Organization-Id` by default, with targeted client coverage proving the default. | `cd frontend && npm test -- --run src/services/__tests__/httpClient.test.ts` | 2026-04-18 |
| 9. Route-health E2E coverage has false positives and contract drift | `partial` | `P4-T7E-DARK`, `P4-T9` | Link-health now fails on same-origin console/network/runtime errors, derives redirect expectations from the admin route manifest, and the current tree cleans up the anonymous route regressions the stricter Chromium pass exposed: `/demo/dashboard`, `/demo/operations`, `/demo/outreach`, `/portal/login`, `/portal/signup`, and `/portal/accept-invitation/test-token` now pass. The remaining blocker is authenticated-route fixture stability: strict admin bootstrap intermittently falls back to invalid cached/default credentials, and the portal fixture setup is still tripping `/api/v2/auth/setup-status` with `500` before the protected portal routes can finish. | `cd frontend && npm test -- --run src/components/__tests__/AdminRoute.test.tsx src/routes/__tests__/adminRedirects.test.tsx src/features/neoBrutalist/pages/__tests__/OperationsBoardPage.test.tsx src/features/neoBrutalist/pages/__tests__/OutreachCenterPage.test.tsx src/services/loop/__tests__/demo.test.ts src/services/__tests__/portalUnauthorizedHandler.test.ts src/services/bootstrap/__tests__/portalBootstrap.test.ts src/features/portal/pages/__tests__/PortalAccessPages.test.tsx src/hooks/__tests__/useNavigationPreferences.test.tsx src/services/__tests__/httpClient.test.ts`; `cd e2e && bash ../scripts/e2e-playwright.sh host ./node_modules/.bin/playwright test tests/link-health.spec.ts --project=chromium --grep "loads /demo/dashboard|loads /demo/operations|loads /demo/outreach"`; `cd e2e && bash ../scripts/e2e-playwright.sh host ./node_modules/.bin/playwright test tests/link-health.spec.ts --project=chromium --grep "loads /portal/login|loads /portal/signup|loads /portal/accept-invitation/test-token"` | — |
| 10. Long-running Playwright audits lose the web server | `partial` | `P4-T7E-DARK`, `P4-T9` | The Playwright wrapper now moves long-running audits onto the compiled preview runtime instead of reusing a fragile dev server, and the narrow host reruns above stayed on the preview-backed harness without the old web-server loss. Full audit closure is still blocked by authenticated-route provisioning instability rather than a preview-runtime drop. | `cd e2e && bash ../scripts/e2e-playwright.sh host ./node_modules/.bin/playwright test tests/link-health.spec.ts --project=chromium --list`; `cd e2e && bash ../scripts/e2e-playwright.sh host ./node_modules/.bin/playwright test tests/link-health.spec.ts --project=chromium --grep "loads /demo/dashboard|loads /demo/operations|loads /demo/outreach"`; `cd e2e && bash ../scripts/e2e-playwright.sh host ./node_modules/.bin/playwright test tests/link-health.spec.ts --project=chromium --grep "loads /portal/login|loads /portal/signup|loads /portal/accept-invitation/test-token"`; `make check-links` | — |
| 11. Frontend production dependencies include the DOMPurify advisory | `closed` | `P4-T45` | The frontend override and root lockfile now resolve `dompurify` to `3.4.0`, and the frontend audit is clean at the moderate threshold. | `cd frontend && npm audit --omit=dev --audit-level=moderate` | 2026-04-18 |
| 12. API key middleware has unsafe-by-default ergonomics | `closed` | `P4-T45` | API-key auth no longer accepts query-string credentials and now enforces rate limits in the request path instead of only incrementing counters. | `node scripts/check-rate-limit-key-policy.ts`; `cd backend && DB_HOST=127.0.0.1 DB_PORT=8012 DB_NAME=nonprofit_manager_test DB_USER=postgres DB_PASSWORD=postgres npx jest src/__tests__/middleware/apiKeyAuth.test.ts --runInBand` | 2026-04-18 |
| 13. Payment webhook freshness rejects valid delayed retries after five minutes | `closed` | `P4-T45` | Payment webhooks no longer reject legitimate retries solely because they are older than five minutes. Replay protection now depends on signature validation plus receipt-table idempotency. | `cd backend && npm test -- --runInBand src/__tests__/integration/routeGuardrails.test.ts -t \"replay and duplicate paths\"` | 2026-04-18 |
| 14. Portal admin authorization bypasses the centralized authorization kernel | `closed` | `P4-T45` | Portal-admin authorization now routes through the shared auth/permission infrastructure instead of a local `role === 'admin'` check, and the auth regression slice is aligned with the membership-based org model. | `cd backend && DB_HOST=127.0.0.1 DB_PORT=8012 DB_NAME=nonprofit_manager_test DB_USER=postgres DB_PASSWORD=postgres npx jest src/__tests__/integration/authorization.test.ts src/__tests__/utils/permissions.test.ts --runInBand` | 2026-04-18 |

## Notes

- Finding `2` is now closed at the backend-proof level: backend lint, backend type-check, the auth/security regression slice, and the cases/reminder integration slice all passed in the current tree. Repo-wide release proof is still broader than this finding and stays tracked in the parent rows.
- Findings `9` and `10` remain implementation-complete but validation-partial because the upgraded route-health browser run now passes the anonymous demo and public portal slices while the remaining failures are concentrated in authenticated E2E provisioning (`auth/setup-status` and strict-admin credential/bootstrap drift), not the route-health harness contract itself.
