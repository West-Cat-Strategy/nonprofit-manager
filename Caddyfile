{
    # Enable admin API for internal use if needed, but not strictly required
    # admin off 
}

localhost {
    # Serve the frontend
    reverse_proxy frontend-dev:8005

    # Use internal CA for this private domain
    tls internal
}
