/**
 * MODULE-OWNERSHIP: payments controller boundary
 *
 * Canonical controller exports and initialization entrypoints are local to this
 * module and are consumed directly by module-aware call sites.
 *
 * @deprecated This file exists as the module entrypoint; keep only exports that
 * map directly to owned controller implementations.
 */

export * from './paymentController';
