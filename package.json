{
  "name": "nonprofit-manager",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "contracts",
    "backend",
    "frontend",
    "e2e"
  ],
  "scripts": {
    "audit": "npm audit --workspaces --include-workspace-root --audit-level=moderate",
    "audit:prod": "npm audit --omit=dev --workspaces --include-workspace-root --audit-level=moderate",
    "build": "make build",
    "clean-local": "make clean-local",
    "knip": "knip --config knip.json --include files,dependencies,unlisted,unresolved,binaries",
    "lint": "make lint",
    "sbom": "npm sbom --sbom-format cyclonedx",
    "test": "make test",
    "typecheck": "make typecheck"
  },
  "engines": {
    "node": ">=20.19.0"
  },
  "overrides": {
    "ajv": "^6.14.0",
    "dompurify": "^3.4.0",
    "exceljs@4.4.0": {
      "uuid": "^14.0.0"
    },
    "flatted": "^3.4.2",
    "handlebars": "^4.7.9",
    "path-to-regexp": "^8.4.0",
    "postcss": "^8.5.14",
    "rollup": "^4.59.0",
    "test-exclude": {
      "minimatch": "3.1.5"
    },
    "glob": {
      "minimatch": "3.1.5"
    },
    "readdir-glob": {
      "minimatch": "5.1.9"
    },
    "@sentry/node": {
      "minimatch": "10.2.5"
    },
    "undici": "^8.2.0",
    "vite": "^8.0.10"
  },
  "devDependencies": {
    "knip": "^6.11.0",
    "yaml": "^2.8.3"
  }
}
